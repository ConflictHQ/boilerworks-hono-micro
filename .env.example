# Cloudflare D1 database name (matches wrangler.toml binding)
D1_DATABASE=boilerworks-db

# API key seed (shown once on first deploy, change in production)
API_KEY_SEED=bw_seed_key_change_me_in_production

# Environment
ENVIRONMENT=development
